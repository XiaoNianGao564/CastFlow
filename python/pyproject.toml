[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "castflow-ml"
version = "0.1.0"
description = "CastFlow Python ML 后端"
requires-python = ">=3.8"
dependencies = [
    "pandas>=1.1.0",
    "numpy>=1.19.0",
    "pymysql>=1.0.0",
    "statsmodels>=0.12.0",
]
